# pymodbus 3.x requires Python 3.8+. Python 3.7 (e.g. Debian buster / older
# Raspberry Pi OS) gets the last 2.x release, which this tool also supports.
pymodbus>=3.0,<4 ; python_version >= "3.8"
pymodbus==2.5.3  ; python_version <  "3.8"
